# How to recreate the Ubuntu VPS server

1. `cd && git clone https://github.com/gbili/dotfiles.git`
2. `~/dotfiles/scripts/link-dotfiles.bash`
3. `exit`
4. `login again to see dotfiles applied`
5. `ubuntu-recreate`
